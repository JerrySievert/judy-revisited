# Experimental Javascript Version

This is a very experimental compiled version of Judy Arrays in Javascript.

For the most part, the implementation is the same, but it is missing `keys()` and `size()`.
